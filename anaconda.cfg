# Generated by Anaconda 38.23.4
# Generated by pykickstart v3.47
#version=F38
# Use graphical install
graphical

# Keyboard layouts
keyboard --vckeymap=us --xlayouts='us'
# System language
lang en_US.UTF-8

# Repository
url --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=fedora-38&arch=x86_64
repo --name=update --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f38&arch=x86_64

%packages
@^minimal-environment
docker

%end

# Network
network --bootproto=dhcp --device=eth0 --onboot=yes --ipv6=auto
network --hostname=fedorav2

# Run the Setup Agent on first boot
firstboot --enable

# Generated using Blivet version 3.7.1
ignoredisk --only-use=sda
autopart
# Partition clearing information
clearpart --none --initlabel

# System timezone
timezone Europe/Warsaw --utc

#Root password
rootpw --lock
user --groups=wheel --name=kuba --password=$y$j9T$MNoMF5r67fhxFu42Xs7qI3wW$.ego15TpEtGja.tOZ4rRcN3o54q3aqyVcUHshEKSoMC --iscrypted --gecos="kuba"

# Run post-installation script
%post --erroronfail

cat << 'EOF' > /etc/systemd/system/my-docker.service

usermod -aG docker kuba2
systemctl enable docker

[Unit]
Description=Download and start docker with app
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=/usr/bin/docker pull deflain01/tonebuild:111
ExecStart=/usr/bin/docker run -di -p 8081:8081 --name tonebuild_container deflain01/tonebuild:111

[Install]
WantedBy=multi-user.target
EOF

systemctl daemon-reload
systemctl enable my-docker.service
systemctl start my-docker.service

%end
